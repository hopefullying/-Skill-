# LICENSING · 授权说明（维护者需确认）

⚠️ **发布前请确认以下三项决策。** 目前的设置是保守默认值。

---

## 决策 1：本仓库的整理成果用什么许可？

**当前默认**：`CC BY-NC-SA 4.0`（署名—非商业性使用—相同方式共享）

选择理由：内容涉及政治立场且含第三方引用，非商业 + 相同方式共享能最大限度降低风险。

| 备选 | 适用场景 | 权衡 |
|---|---|---|
| `CC BY-NC-SA 4.0`（当前） | 不希望被商用 | 限制传播，不能用于商业产品 |
| `CC BY 4.0` | 希望最大化传播 | 允许商用，但你可能不希望对政治内容如此 |
| `CC BY-SA 4.0` | 允许商用但要求同样开放 | 折中 |
| `MIT` | 纯代码风格项目 | **不建议**：不适合含第三方引用的文本内容 |

**改法**：替换 `LICENSE` 文件内容，并同步更新 `README.md` 的 license 徽章。

---

## 决策 2：直接引用是否保留？✅ **已采用方案 C（只留出处、不留原话）**

**发布版状态：已移除全部原话引用。** 16 个 skill 的 `## R` 段已改为「出处坐标」；
DIGEST 等文档中的引文块已改为出处行。溯源仍然完整（见 `SOURCES.md`）。

**风险**：这些是原讲者的口语表达，著作权归其所有。虽然短引用 + 评论/研究用途
在多数法域下属于合理使用范畴，但**不能保证**。

| 方案 | 做法 | 代价 |
|---|---|---|
| A. 保留引用（未采用） | 出处见 `SOURCES.md` | 有被要求下架的理论风险 |
| B. 保留但打码 | 把引文中的粗话/贬称替换为 `***` | 损失原味，但降低不适感 |
| C. 只留出处不留原话 | 把每条的"R — 原文"替换为"R — 出处：<文件> :: <时间戳>"，删掉原文 | **最安全**，但溯源价值下降 |

**已按方案 C 执行**，适配公开发布。若日后要恢复引用，模板保留在 `work/backup_release_with_quotes/`（不在发布包内）。

---

## 决策 3：署名与仓库地址

需你补上（当前为中性占位）：

- `.codex-plugin/plugin.json` → `author.name`、`interface.developerName`
- `README.md` → 两处 `<your-repo-url>`

---

## 附：为什么不包含原始语料

原始转写全文（148.6 万字符）**不属于可发布内容**：那是原讲者作品的完整复制，
与"短引用 + 研究性提炼"是两个性质。公开分发的只应是提炼成果。
